export enum ChatRoomType {
  DEFAULT = 'DEFAULT',
  PRIVATE = 'PRIVATE',
  DM = 'DM',
}

export enum MessageType {
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
  FILE = 'FILE',
  SYSTEM = 'SYSTEM',
}